# ============================================================================
# ШАБЛОН CONFIGMAP ДЛЯ K3s
# ============================================================================
# ВАЖНО: Этот файл является шаблоном и НЕ используется напрямую!
# ConfigMap создается автоматически из .prod.env через скрипт create-secrets.sh
# 
# Для создания ConfigMap из .prod.env выполните:
#   ./k3s/create-secrets.sh
#
# Или создайте ConfigMap вручную:
#   kubectl create configmap booking-config \
#     --from-literal=DB_HOST=postgres-service \
#     --from-literal=ROOT_PATH=/apps/shum-booking \
#     ... (и т.д.)
#     --namespace=booking

apiVersion: v1
kind: ConfigMap
metadata:
  name: booking-config
  namespace: booking
data:
  # Все значения берутся из .prod.env через скрипт create-secrets.sh
  # Этот файл служит только как документация структуры ConfigMap

